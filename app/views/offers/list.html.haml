%h2 Fresh offers
= "Verification response signature result: #{@verified}, "
= link_to "get even fresher offers (back)", :controller => "offers", :action => "get"
%br
%br

- if @offers.empty?
  .no_offers
    No offers

- if !@offers.empty?
  - @offers.each do |offer|
    .offer.one
      .title
        = "Offer: #{offer.title}"
      .payout
        = "Payout: #{offer.payout}"
      .thumbnail
        = link_to "Hi resolution thumbnail", offer.thumbnail['lowres']
        %br
        = link_to "Low resolution thumbnail", offer.thumbnail['hires']
        %br
        Preview
        %br
        = image_tag offer.thumbnail["lowres"]
    %br
